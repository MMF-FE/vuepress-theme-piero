@import "var.pcss";

.pr-header-logo {
    display: flex;
    align-items: center;
    margin-right: 20px;

    /deep/ {
        img {
            display: block;
            width: 100px;
            height: auto;
        }
    }
}
